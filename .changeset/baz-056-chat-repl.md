---
'bazilion': minor
---

BAZ-056: the interactive chat REPL is removed from `bazilion agent chat`; one-shot mode (`--message`, `--image`, `--file`), piped stdin (fail-closed `auto_deny`), and the chat management commands remain.
