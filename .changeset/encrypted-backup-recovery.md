---
'bazilion': minor
---

Add recipient-encrypted age backups, authenticated staged restore, secret-name-only credential
inventory, offline crash-safe bootstrap rotation with non-bootstrap token revocation, and an explicit
Telegram/OpenAI/provider recovery checklist. Plaintext backup creation now requires `--plaintext`.
