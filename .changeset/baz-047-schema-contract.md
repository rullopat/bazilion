---
'bazilion': minor
---

BAZ-047: stable schema contract — homes upgrade in place. Forward-only prefix migrations with a receipt ledger, numeric refuse-newer (`PRAGMA user_version`), automatic verified pre-migration snapshots beside the database, and a CI release-upgrade matrix proven against real prior releases. Retires the alpha clean-install (wipe-on-change) contract.
